<Window x:Class="BoxPackingCalculator.App.Views.PackingPrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:BoxPackingCalculator.App.Views"
        Title="인쇄 미리보기"
        Width="1250"
        Height="900"
        MinWidth="900"
        MinHeight="650"
        WindowStartupLocation="CenterOwner">

    <Grid Background="#E5E5E5">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- 상단 도구 영역 -->
        <Border
            Grid.Row="0"
            Padding="16,10"
            Background="White"
            BorderBrush="#D0D0D0"
            BorderThickness="0,0,0,1">

            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Right">

                <Button
                    Width="120"
                    Height="36"
                    Content="인쇄"
                    FontWeight="SemiBold"
                    Click="PrintButton_Click"/>

            </StackPanel>

        </Border>


        <!-- A4 미리보기 -->
        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto">

            <ItemsControl
                x:Name="PageItemsControl"
                Margin="24"
                HorizontalAlignment="Center">

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <Border
                            Margin="0,0,0,24"
                            Background="White">

                            <views:PackingPrintView/>

                        </Border>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </ScrollViewer>

    </Grid>

</Window>